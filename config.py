# Maximum number of characters to return when reading a file
MAX_CHARS = 10000
WORKING_DIR = "./calculator"
MAX_ITERS = 20

# List of files the AI should never modify/execute
BLOCKED_FILES = {
    "main.py",
    "config.py",
    "prompts.py",
    "call_function.py",
}
